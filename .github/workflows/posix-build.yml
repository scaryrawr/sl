name: Posix Build SL
on:
  workflow_call:

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-13]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build Debug
        run: cargo build

      - name: Build Release
        run: cargo build --release

      - name: Test
        run: |
          ./target/debug/sl -h
          ./target/debug/sl -V
          ./target/release/sl -h
          ./target/release/sl -V
